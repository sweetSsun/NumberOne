<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
 <mapper namespace ="com.NumberOne.dao.MemberDao">


<!-- 	<insert id="insertRegisterWrite"  >
		INSERT INTO MEMBERS(MID, MPW, MNAME, MNICKNAME, MPHONE, MEMAIL, MADDR, MPROFILE, MMESSAGE, MJOINDATE ) 
		VALUES (#{mid}, #{mpw}, #{mname}, #{mnickname}, #{mphone}, #{memail}, #{maddr}, #{mprofile}, #{mmessage}, SYSDATE )
	</insert> -->

<!-- 	<select id="selectMemberId_ajax" resultType="com.NumberOne.dto.MemberDto">
		SELECT MID FROM MEMBERS WHERE MID = #{inputId}
	</select> -->
	
	
	<!--
	<select id="selectMemberId_ajax" resultType="com.NumberOne.dao.MemberDto">
		select mid, mstate 
		from members 
		where mname = #{checkMname}
		and memail = #{checkMemail}
	</select>
	-->
	
	
</mapper>


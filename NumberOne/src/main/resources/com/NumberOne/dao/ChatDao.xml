<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
<mapper namespace="com.NumberOne.dao.ChatDao">

	
	
	<select resultType="com.NumberOne.dto.ChatDto" id="selectChatList">
	SELECT CHCODE, CHFRMID, CHTOMID, CHCONTENTS, TO_CHAR(CHDATE,'YYYY-MM-DD HH24:MI') AS CHDATE
	FROM CHAT ORDER BY CHDATE
	</select>
	
	<select resultType="int" id="selectMaxChcode">
	SELECT NVL(MAX(CHCODE),0)
	FROM CHAT
	</select>
	
	<insert id="insertChat">
	INSERT INTO CHAT (CHCODE, CHFRMID, CHTOMID, CHCONTENTS, CHDATE)
	VALUES (#{chcode}, #{chfrmid}, #{chtomid}, #{chcontents}, sysdate)
	</insert>
	
	<select resultType="String" id="selectMfrnick">
	SELECT MNICKNAME
	FROM MEMBERS
	WHERE MID = #{chfrmid}
	</select>
	
	<select resultType="String" id="selectMtonick">
	SELECT MNICKNAME
	FROM MEMBERS
	WHERE MID = #{chtomid}
	</select>
	
	<select resultType="String" id="selectTime">
	SELECT TO_CHAR(CHDATE, 'YYYY-MM-DD AM HH:MI')
	FROM CHAT
	WHERE CHCODE = #{chcode}
	</select>

	<!-- 여기부터 새로한거! -->
	<!-- chatRoom 조회 -->
	<select id="selectChatRoom" resultType="com.NumberOne.dto.ChatRoomDto">
		SELECT * 
		FROM CHATROOM
		WHERE CRCODE = #{cmcrcode}
	</select>
	
	<!-- crcode 최대값 조회 -->
	<select id="selectMaxCrcode" resultType="String">
		SELECT NVL(MAX(CRCODE), CR00000)
		FROM CHATROOM
	</select>
	
	<!-- chatRoom 생성 -->
	<insert id="insertChatRoom">
		INSERT INTO CHATROOM
		VALUES ()
	</insert>
</mapper>
